{
	"name": "chitter-challenge-backend",
	"version": "1.0.0",
	"description": "backend section for the chitter challenge",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "NODE_ENV=test mocha --timeout 10000 --exit",
		"start": "NODE_ENV=development nodemon src/server",
		"start-prod": "NODE_ENV=production nodemon src/server",
		"test-win": "SET NODE_ENV=test&&mocha --timeout 10000 --exit",
		"start-win": "SET NODE_ENV=development&&nodemon src/server",
		"start-prod-win": "SET NODE_ENV-production&&nodemon src/server"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/elleordona/chitter-challenge.git"
	},
	"author": "Elle Ordoña",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/elleordona/chitter-challenge/issues"
	},
	"homepage": "https://github.com/elleordona/chitter-challenge#readme",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.0.3",
		"express": "^4.18.2",
		"express-validator": "^6.15.0",
		"mongoose": "^7.0.2"
	},
	"devDependencies": {
		"chai": "^4.3.7",
		"chai-http": "^4.3.0",
		"mocha": "^10.2.0"
	}
}
